class PersonController {
    // List all registers
    static async index(req, res) {
        try {

        } catch (error) {

        }
    }
}

// Singleton instance
module.exports = PersonController